<main class="deck-container w-50">
    <div class="row m-2">
      <table>
        <tr>
          <th>Deck Name</th>
          <th>Description</th>
          <th>Author</th>
        </tr>
        {{#each decks}}
          {{> deck}}
        {{/each}}
    </table>
  </div>
</main>

{{> newdeckmodal}}
